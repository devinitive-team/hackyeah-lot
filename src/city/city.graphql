type City {
    id: String,
    advertisment: String,
    category: String,
    code: String,
    description: String,
    name: String,
    imagesSmall: [String],
    imagesBig: [String],
}

type Query {
    cities: [City]
    city(id: ID!): City
}
